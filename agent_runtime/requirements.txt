# =============================================================================
# AgentForge Agent Runtime — Dépendances Python
# Installées dans la microVM Kata Container
# =============================================================================

# LangGraph — Orchestration de graphes d'agents stateful
langgraph==0.2.55
langgraph-checkpoint-sqlite==2.0.1

# LangChain core (utilisé par LangGraph)
langchain-core==0.3.25
langchain==0.3.9

# Adaptateurs LLM
langchain-anthropic==0.3.3
langchain-openai==0.2.10

# LangFuse — Tracing & observabilité (callback handler)
langfuse==2.57.3

# HTTP client (pour les appels Git API dans le container)
httpx==0.27.2
requests==2.32.3

# Utilitaires
python-dotenv==1.0.1
structlog==24.4.0
pydantic==2.10.3

# Git (interface Python — fallback si git CLI absent)
GitPython==3.1.43
