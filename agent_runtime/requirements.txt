# =============================================================================
# AgentForge Agent Runtime — Dépendances Python
# Versions testées sur Python 3.11 / Ubuntu 22.04 — Mars 2026
# =============================================================================

# LangGraph — Orchestration de graphes d'agents stateful
langgraph>=1.1.2
langgraph-checkpoint-sqlite>=3.0.3

# LangChain (laisse pip résoudre les versions compatibles)
langchain>=1.2.0
langchain-core>=1.2.0

# Adaptateurs LLM
langchain-anthropic>=1.3.0
langchain-openai>=1.1.0

# LangFuse v4 — Tracing & observabilité (callback handler)
langfuse>=4.0.0

# HTTP client (pour les appels Git API dans le container)
httpx>=0.27.0
requests>=2.32.0

# Utilitaires
python-dotenv>=1.0.0
structlog>=24.0.0
pydantic>=2.0.0

# Git (interface Python — fallback si git CLI absent)
GitPython>=3.1.0
